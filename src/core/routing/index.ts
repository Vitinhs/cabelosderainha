/**
 * Routing — Proteção de rotas e features por auth e plano
 *
 * @example
 * import { PrivateRoute, PlanGuard } from 'src/core/routing'
 */
export { PrivateRoute } from './PrivateRoute';
export { PlanGuard } from './PlanGuard';
